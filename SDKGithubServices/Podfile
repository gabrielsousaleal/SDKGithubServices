# Uncomment the next line to define a global platform for your project
# platform :ios, '14.0'

target 'SDKGithubServices' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SDKGithubServices

  target 'SDKGithubServicesTests' do
    # Pods for testing
  end

end

target 'SDKGithubServicesSample' do
  use_frameworks!
end
